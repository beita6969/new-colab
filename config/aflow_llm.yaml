# AFlow LLM配置
# 用于执行工作流中的算子调用

models:
  "gpt-oss-120b":
    api_type: "openai"
    base_url: "http://localhost:8002/v1"  # 使用本地vLLM 8002端口
    api_key: "sk-dummy"  # vLLM不需要真实key
    model_name: "/home/yijia/lhy/openai/gpt-oss-120b"  # 使用完整路径匹配vLLM
    temperature: 0
    top_p: 1
    max_tokens: 4096  # P0修复: 防止不完整代码
    no_proxy: "localhost,127.0.0.1"  # 绕过代理

  "gpt-4o-mini-remote":
    api_type: "openai"
    base_url: "https://api.openai.com/v1"
    api_key: "YOUR_OPENAI_API_KEY"
    temperature: 0
    top_p: 1
